// https://en.wikipedia.org/wiki/Julia_set
// Formula for normal Julia sets: `f(z) = z² + c`

use num::complex::Complex;

pub fn julia(z: Complex, c: Complex, escape_radius: u32, max_iterations: u32) -> u32 {
    let mut iterations: u32 = 1;

    // While 
    // Implement it!

    return iterations;
}
