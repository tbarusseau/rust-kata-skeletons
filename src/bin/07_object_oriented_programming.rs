// Seen in this chapter:
// - Rust is not OOP! No "class" inheritance
// - Methods implementations
// - Traits
// - Trait inheritance
// - Static and dynamic dispatch (+ trait objects)
// - Parameterized generic types
// - Box

// Exercise: create a `Add` trait, which adds itself to a parameter.
// Exercise: `impl` the `Add` trait to a number type, for example i32.

// Not really inspired: Rustlings?

fn main() {}
